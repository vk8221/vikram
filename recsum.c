#include<STDIO.H>
int sum(int)
int main()
{
    int num, sum=0;
    printf("\nenter a number");
    scanf("%d",&num);
    sum();
    printf("sum of natural number =%d",sum);  
    return 0;
}
int sum(int i)
{
   for(i=1;i<=num;i++)
    {
        sum=sum+i;
            }
    
}
 